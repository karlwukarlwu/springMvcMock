Class<?> 是 Java 中的一种泛型类型声明，用于表示一个未知的 Java 类型。
它通常用于泛型代码中，表示可以接受任何类型的 Class 对象作为参数或返回值，而不需要具体指定类型。


Class<?>:

Class<?> 是一个泛型类型，用于表示一个未知的 Java 类型的 Class 对象。
它通常用于编写泛型代码或使用反射来操作未知类型的类。
通过 Class<?>，你可以获取有关类的信息，例如类的名称、方法、字段等，而不需要具体指定类的类型。
用于在编译时不知道具体类类型的情况下进行类型安全的操作。

Object:

Object 是 Java 中的一个类，是所有其他类的基类。每个类都直接或间接继承自 Object。
Object 是一个具体的类，可以用来引用任何 Java 对象，因为它是所有类的父类。
当你不知道具体类型的对象时，可以使用 Object 来引用它，但这通常会导致类型安全性的问题，因为你需要进行类型转换才能访问对象的具体方法和属性。